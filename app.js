document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener("click",e=>{const id=a.getAttribute("href");if(id&&id!=="#"){const el=document.querySelector(id);if(el){e.preventDefault();el.scrollIntoView({behavior:"smooth",block:"start"})}}}));const q=document.querySelector("#search");if(q)q.addEventListener("input",()=>{const term=q.value.toLowerCase();document.querySelectorAll(".cards article").forEach(c=>c.style.display=!term||c.innerText.toLowerCase().includes(term)?"":"none")});